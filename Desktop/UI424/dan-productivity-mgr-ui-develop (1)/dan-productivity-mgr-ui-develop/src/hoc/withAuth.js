// import React from 'react';
// import { AuthContext } from '@dentsu/platform-shell';

// const withAuth = props => WrappedComponents => nextProps => (
//   <AuthContext.Consumer>
//     {auth => <WrappedComponents {...props} {...auth} {...nextProps} />}
//   </AuthContext.Consumer>
// );

// export default withAuth;

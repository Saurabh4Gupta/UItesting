// import React from 'react';
// import { CmsContext } from '@dentsu/platform-shell';

// const withCms = props => WrappedComponent => () => (
//   <CmsContext>
//     {cmsData => <WrappedComponent {...props} {...cmsData} />}
//   </CmsContext>
// )

// export default withCms;
